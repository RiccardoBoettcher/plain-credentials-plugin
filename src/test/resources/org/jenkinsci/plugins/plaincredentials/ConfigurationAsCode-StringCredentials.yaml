credentials:
  system:
    domainCredentials:
    # global credentials
    - credentials:
      - string:
          description: "secret-text-description"
          id: "secret-text-1"
          scope: GLOBAL
          secret: "very-secret"
